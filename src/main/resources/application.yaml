# ============================================
# QUARKUS CONFIG 
# ============================================
quarkus:
  http:
     host: 192.168.1.77
     port: 8080

  log:
    level: INFO
    min-level: TRACE
      
    category:
      # For configuration property names that use quotes, do not split the string inside the quotes
      "kz.kase.example":
        level: TRACE

  swagger-ui:
    always-include: true